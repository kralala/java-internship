version: "3.7"
services:
    image: postgres:alpine
    container_name: container
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: school
      POSTGRES_USER: user
      POSTGRES_PASSWORD: admin
