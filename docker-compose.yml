version: "3.7"
services:
  postgres:
    image: postgres:alpine
    environment:
      POSTGRES_DB: social
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"