version: "3"

services:
  db:
    image: postgres:alpine
    container_name: container
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=social_db
    ports:
      - "5432:5432"